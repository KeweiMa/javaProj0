app:
  packageName: @project.artifactId@-@project.version@-@build.time@.jar
  build:
    time: @build.time@
spring:
  application:
    name: kma-project0
  http:
    encoding: { charset: UTF-9, enable: true, force: true }
  cloud:
    config:
      uri: http://localhost:8762